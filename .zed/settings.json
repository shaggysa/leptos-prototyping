{
    "vim_mode": true,
    "autosave": "on_focus_change",
    "lsp": {
        "rust-analyzer": {
            "initialization_options": {
                "cargo": {
                    "features": ["ssr"] // Enable only the ssr feature
                    // OR
                    // "allFeatures": true   // Enable all features if you want everything enabled
                }
            }
        }
    }
}
